import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:["image.unsplash.com", "images.unsplash.com", "img.clerk.com", "ik.imagekit.io"],
  },
};

export default nextConfig;
